/**
 * Canonical CV types (also exported from `lib/types/cv.ts` for the app).
 */
export type {
  PersonalInfo,
  CvDocument,
  TailoredCV,
  ExtraProjectInput,
  TailorRequestBody,
  CVTemplateId,
} from "@/lib/types/cv";

export type {
  ExperienceItem as Experience,
  ProjectItem as Project,
  EducationItem as Education,
  CertificationItem as Certification,
} from "@/lib/types/cv";
